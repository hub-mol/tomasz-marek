---
import type { Post } from '../../data/blog';
import { excerpt } from '../../data/blog';

// Dwa układy listy wpisów z dema:
//   large   — No Sidebar: jedna kolumna, duże zdjęcie, zajawka
//   compact — Blog Compact: siatka 3 kolumn, bez zajawki
interface Props {
  posts: Post[];
  layout?: 'large' | 'compact';
}

const { posts, layout = 'large' } = Astro.props;
---

<div class="container">
  <ul class:list={['feed', `feed--${layout}`]}>
    {
      posts.map((post) => (
        <li>
          <article class="post">
            <a class="post__media" href="#">
              <img
                src={post.img}
                alt=""
                width="1100"
                height="692"
                loading="lazy"
              />
              <span class="post__date type-label">
                <span class="post__day">{post.day}</span>
                <span class="post__month">{post.month}</span>
              </span>
            </a>

            <div class="post__body">
              <p class="post__cats type-label">
                {post.categories.map((c) => (
                  <span>{c}</span>
                ))}
              </p>

              <h2 class:list={['post__title', layout === 'large' ? 'type-h3' : 'type-heading-lg']}>
                <a href="#">{post.title}</a>
              </h2>

              {layout === 'large' && <p class="post__excerpt">{excerpt}</p>}
            </div>
          </article>
        </li>
      ))
    }
  </ul>
</div>

<style>
  .feed--large {
    display: grid;
    gap: var(--space-16);
  }

  .feed--compact {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-12) var(--space-4);
  }

  .post__media {
    position: relative;
    display: block;
    overflow: hidden;
  }

  .post__media img {
    width: 100%;
    height: auto;
    aspect-ratio: 1100 / 692;
    object-fit: cover;
    transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .post__media:hover img {
    transform: scale(1.04);
  }

  /* data w białym kaflu na rogu zdjęcia — jak w oryginale */
  .post__date {
    position: absolute;
    top: 0;
    left: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 3.75rem;
    height: 5.0625rem;
    background: var(--c-bg);
  }

  .post__day {
    font-family: var(--ff-heading);
    font-weight: 700;
  }

  .post__body {
    padding-top: var(--space-8);
  }

  .post__cats {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-1) var(--space-4);
    color: var(--c-muted);
  }

  .post__title {
    margin-top: var(--space-2);
  }

  .post__title a {
    transition: color 0.3s ease;
  }

  .post__title a:hover {
    color: var(--c-accent);
  }

  .post__excerpt {
    margin-top: var(--space-4);
    max-width: 60ch;
    color: var(--c-muted);
  }

  @media (max-width: 1024px) {
    .feed--large {
      gap: var(--space-12);
    }

    .feed--compact {
      grid-template-columns: repeat(2, 1fr);
    }

    .post__body {
      padding-top: var(--space-6);
    }
  }

  @media (max-width: 768px) {
    .feed--compact {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }
  }
</style>
