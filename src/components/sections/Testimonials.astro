---
// Opinia klienta. Treść jest na razie przykładowa — do podmiany na prawdziwą.
interface Props {
  text?: string;
  name?: string;
  job?: string;
  photo?: string;
  alt?: boolean;
  demo?: boolean;
}

const {
  text = 'Najbardziej doceniam to, że na każdym etapie wiedzieliśmy, co się dzieje i dlaczego. Zamiast gotowego katalogu rozwiązań dostaliśmy projekt, który naprawdę odpowiada na to, jak żyjemy.',
  name = 'Anna i Paweł',
  job = 'dom jednorodzinny pod Gdańskiem',
  photo = '/img/team-image-05.jpg',
  alt = false,
  demo = false,
} = Astro.props;
---

<section class:list={['section', 'testimonial', 'split-golden', { 'section--alt': alt }]}>
  <figure class="testimonial__inner">
    {demo && <p class="testimonial__demo tagline">Opinia demonstracyjna</p>}
    <blockquote>
      <p class="testimonial__text">&bdquo;{text}&rdquo;</p>
    </blockquote>

    <figcaption class="testimonial__author">
      {
        photo && (
          <img
            class="testimonial__photo"
            src={photo}
            alt=""
            width="72"
            height="72"
            loading="lazy"
          />
        )
      }
      <span class="testimonial__who">
        <span class="testimonial__name">{name}</span>
        <span class="testimonial__job">{job}</span>
      </span>
    </figcaption>
  </figure>
</section>

<style>
  .testimonial {
    padding-top: 6.875rem;
    padding-bottom: calc(6.875rem + var(--sec-y));
  }

  .testimonial__inner {
    grid-column: 2;
    margin: 0;
  }

  .testimonial__text {
    max-width: 43.75rem;
    margin-bottom: var(--space-8);
    font-family: var(--ff-heading);
    font-size: var(--text-lg);
    line-height: var(--leading-xl);
    letter-spacing: var(--tracking-tight);
  }

  .testimonial__demo {
    margin-bottom: var(--space-4);
  }

  /* zdjęcie stoi przy autorze, nie przy całym cytacie */
  .testimonial__author {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    font-size: var(--text-md);
    line-height: var(--leading-sm);
  }

  .testimonial__photo {
    flex-shrink: 0;
    width: 4.5rem;
    height: 4.5rem;
    object-fit: cover;
  }

  .testimonial__name {
    display: block;
    font-family: var(--ff-heading);
    font-weight: 700;
  }

  .testimonial__job {
    color: var(--c-muted);
  }

  @media (max-width: 1024px) {
    .testimonial {
      padding-block: 5.625rem;
      padding-bottom: calc(5.625rem + var(--sec-y));
    }

    .testimonial__inner {
      grid-column: auto;
    }
  }

  @media (max-width: 768px) {
    .testimonial {
      padding-block: 4.375rem;
      padding-bottom: calc(4.375rem + var(--sec-y));
    }

    .testimonial__photo {
      width: 3.75rem;
      height: 3.75rem;
    }
  }
</style>
