---
interface Props {
  title: string;
  img?: string;
}

const { title, img = '/img/tm-4.jpg' } = Astro.props;
---

<section class="banner">
  <img
    class="banner__media"
    src={img}
    alt=""
    width="1920"
    height="1080"
    loading="lazy"
  />
  <div class="banner__inner">
    <h2 class="banner__title type-h2">{title}</h2>
  </div>
</section>

<style>
  .banner {
    position: relative;
    height: min(38.75rem, 72svh);
    display: flex;
    align-items: center;
    padding-inline: var(--gutter);
    overflow: hidden;
    color: var(--c-on-dark);
  }

  .banner__media {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    max-width: none;
    object-fit: cover;
  }

  .banner::before {
    content: '';
    position: absolute;
    inset: 0;
    background: color-mix(in srgb, var(--c-bg-dark-canvas) 20%, transparent);
  }

  .banner__inner {
    position: relative;
    z-index: 1;
    max-width: 73.75rem;
  }

  .banner__title {
    max-width: 73.75rem;
    white-space: pre-line;
  }

  @media (max-width: 1024px) {
    .banner {
      height: 26.25rem;
    }
  }

  @media (max-width: 768px) {
    .banner {
      height: 21.25rem;
    }

  }
</style>
